node_modules/
.shadow-cljs/
.cpcache/
# shadow-cljs build output (regenerate: amu compile --target wasm32-browser app,
# or kbb -M:cljs -m shadow.cljs.devtools.cli compile app).
# vendor/kotoba-ui.css IS tracked (worker assets serve from web/dist).
web/dist/js/

# nbb dependency cache -- a build artifact, not source
.nbb/
